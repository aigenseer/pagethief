import ContentScriptWorker    from "../../src/lib/class/ContentScriptWorker";
import mockAxios from 'jest-mock-axios';

describe('ContentScriptWorker', () => {
   

    // it('Test fetchData', async () => {
    //     const TEST_DATA = {
    //         param: {    
    //             link: "http://example.js",
    //             type: "base64"
    //         }
    //     };
    
    //     expect(mockAxios.get).toHaveBeenCalledWith(TEST_DATA.param.link);
    
    //     const responseObj = { data: "server says hello!" };
    //     mockAxios.mockResponse(responseObj);

    //     ContentScriptWorker.fetchData(TEST_DATA.param.link, TEST_DATA.param.type as "base64");

    // });

});